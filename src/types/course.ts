export interface Course {
  coverImage: string;
  description: string;
  id: string;
  name: string;
  teacher: string;
  ownerId: string;
  studentCount: number;
  chapterCount: number;
  progressPercentage: number;
}
